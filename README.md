# Resume

This tool generates a resume from JSON data. TypeScript scripts process the JSON files located in the `/data` directory and convert them into LaTeX files. These LaTeX files are then compiled into a PDF using [Latexmk][1], with the final output placed in the `/release` directory.

[1]: https://mgeier.github.io/latexmk.html
